;;; lisp/init-format.el -*- lexical-binding: t; -*-


(use-package format-all
    :straight t
    :ensure t
    :commands (
         format-all-mode
         format-all-buffer
         ))

(provide 'init-format)
;;; init-format.el ends here